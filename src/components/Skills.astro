---
import profile from '../data/profile.en.json';
---

<section id="skills" class="max-w-4xl mx-auto py-8 pb-32 md:pb-16 animate-fade-in">
  <h2
    id="sk-title"
    data-i18n="sections.skills_title"
    class="text-3xl font-bold mb-8 bg-gradient-to-r from-miku-teal-strong dark:from-miku-cyan to-miku-glow dark:to-miku-magenta text-transparent bg-clip-text"
  >
    {profile.sections.skills_title}
  </h2>

  <div class="space-y-12 mt-12">
    {
      Object.entries(profile.skills).map(([category, skills]: [string, any]) => {
        if (typeof skills === 'string') return null;

        const categoryTitle = skills.title || '';
        const categoryExpertise = skills.expertise || '';
        const expertTools = skills.proficient_tools || [];
        const workingTools = skills.familiar_tools || [];

        return (
          <div class="skill-category" data-animate>
            <h3
              id={`sk-${category}_title`}
              class="text-2xl font-semibold text-miku-teal-strong dark:text-miku-cyan mb-4"
            >
              <span data-i18n={`skills.${category}.title`}>{categoryTitle}</span>
              {categoryExpertise && (
                <span class="ml-2 text-sm font-normal text-gray-500 dark:text-gray-400">— <span data-i18n={`skills.${category}.expertise`}>{categoryExpertise}</span></span>
              )}
            </h3>

            <div class="mb-6">
              <p data-i18n="sections.daily" class="text-sm text-miku-text-dim dark:text-miku-dark-dim mb-2 font-medium">
                {profile.sections.daily}
              </p>
              <div class="flex flex-wrap gap-2">
                {expertTools.map((tool: string) => (
                  <span class="px-3 py-1.5 text-sm rounded-lg bg-miku-cyan/10 dark:bg-miku-cyan/15 text-miku-cyan dark:text-miku-glow border border-miku-cyan/30 dark:border-miku-cyan/25 font-medium hover:border-miku-cyan transition-all">
                    {tool}
                  </span>
                ))}
              </div>
            </div>

            {workingTools.length > 0 && (
              <div class="mb-2">
                <p data-i18n="sections.familiar" class="text-sm text-miku-text-dim dark:text-miku-dark-dim mb-2 font-medium">
                  {profile.sections.familiar}
                </p>
                <div class="flex flex-wrap gap-2">
                  {workingTools.map((tool: string) => (
                    <span class="px-3 py-1.5 text-sm rounded-lg bg-gray-100 dark:bg-white/5 text-miku-text-main dark:text-miku-dark-text border border-gray-500/40 dark:border-gray-600/40 hover:border-miku-cyan transition-all">
                      {tool}
                    </span>
                  ))}
                </div>
              </div>
            )}
          </div>
        );
      })
    }

    <div class="skill-category border-t border-gray-500/30 dark:border-miku-dark-border/30 pt-8" data-animate>
      <h3
        id="sf-title"
        data-i18n="sections.soft_skills"
        class="text-3xl font-bold bg-gradient-to-r from-miku-teal-strong dark:from-miku-cyan to-miku-glow dark:to-miku-magenta text-transparent bg-clip-text mb-6"
      >
        {profile.sections.soft_skills}
      </h3>
      <ul class="space-y-4">
        {profile.skills.soft_skills.map((skill, j) => (
          <li class="flex items-start gap-3 text-miku-text-main dark:text-miku-dark-text">
            <span class="mt-1.5 w-2 h-2 rounded-full bg-miku-cyan flex-shrink-0"></span>
            <span data-i18n={`skills.soft_skills.${j}`}>{skill}</span>
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>
